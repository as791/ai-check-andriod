<?xml version="1.0" encoding="utf-8"?>
<!-- android:allowBackup is false, so this only matters on very old backup agents. -->
<full-backup-content>
    <exclude domain="database" />
    <exclude domain="file" />
</full-backup-content>
