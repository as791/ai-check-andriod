# Tooling dependencies for tools/evaluate.py and tools/convert_model.py.
# Not used by the Android app itself — these are offline, maintainer-run scripts.
onnxruntime>=1.18,<2
pillow>=10,<12
numpy>=1.26,<3
torch>=2.2,<3
timm>=1.0,<2
